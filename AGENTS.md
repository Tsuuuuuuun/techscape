# Personal knowledge base — invariants

この vault は、技術的な疑問を調べ、自分の理解を再利用できる形で残すためのもの。ユーザーの現在の依頼・明示した制約を優先する。本書は共通規則、Skills は作業手順、90 Templates は記述の足場を担う。

## 設計思想 — Rust RFC

techscape の根幹には、Rust RFC（Request for Comments）の技術説明・設計議論の考え方がある。エージェントは Rust RFC の Motivation、Guide-level explanation、Reference-level explanation、Rationale and alternatives、Drawbacks、Unresolved questions を、理解を整理する際の手掛かりとして参照する。

技術を学ぶときは、解こうとしている問題と動機を起点に、利用者から見たモデル、内部の仕組み、設計判断の理由、トレードオフ、残る問いへと理解をつなげる。この観点を、Issue では問題と制約、Concept では説明に使える原理や仕組み、Technology では具体的な設計とその理由、Survey では複数の知見を横断する論旨として表現する。各ノートでは、現在の問いに役立つ観点を選び、必要な深さで記述する。

## 構成と分類

| 場所 | 役割 | タイトル |
|---|---|---|
| 10 Issues | 特定技術を離れても成立する一般化された technical problem | 自然文の How ...? |
| 20 Concepts | 再利用できる idea / mechanism / principle / mental model | 名詞・名詞句 |
| 30 Technologies | 具体的な named implementation / specification / protocol / language / system / technique | 固有の名称 |
| 40 Surveys | 複数ノートを論旨に沿って再構成した synthesis | 領域と論点が分かる題名 |
| 90 Templates | 上記のテンプレート | 固定 |

Skills は運用の作業手順を担う。Concept はフラットに配置し、必要な関係は本文のリンクで説明する。Survey はグラフの一部分を読む順序に組み直した成果物。

## 分類の判断

- Issue は問題の主体・望ましい状態・制約を説明でき、複数の解法や設計判断が考えられるもの。「How does MCP discover tools?」は MCP の節で扱う。「How should clients discover available capabilities?」なら Issue 候補になる。単に固有名詞を取り除いて問題を水増ししない。
- 「What is X?」は通常 X の Concept / Technology が答える疑問。対応する Issue を追加しない。
- Concept は汎用の説明原理、Technology は特定の設計・仕様・実装を対象とする。名前があるだけでは Technology としない。Reference Counting の一般原理は Concept、特定の処理系における実現はその Technology の節。境界が曖昧なら現在説明したい対象に合わせ、一つのノートから始める。
- 一度の対話から全種類を作る必要はない。未知の単語は自動的に新規ノートにしない。まず既存ノートの節に追記し、独立した説明と再参照の用途がある場合に分割する。

## タイトル・メタデータ・リンク

本文は日本語を基本とし、名称は標準的な表記を使う。Issue の H1 は英語の How ...? とし、ファイル名では末尾の ? を省く。例: `10 Issues/How should memory be managed.md`。他のファイル名でも `/ : * ? " < > | # %` を避ける。`#` はリンクのフラグメント区切り、`%` はパーセントエンコーディングと衝突し、Markdown リンクを壊す。Concept / Technology のファイル名は名称を基本にし、同名衝突時のみ対象を添える。

Issue・Concept・Technology の知識ノートでは、最小メタデータを type と created（YYYY-MM-DD）とする。この規則は README.md、SKILL.md、Typst の Survey には適用しない。aliases は略称・別表記があるときだけ記入。status は draft / reviewed を使える。reviewed は今回のスコープで確認した意味であり、完全性を保証しない。reviewed のノートに未確認の説明・主張を追加した場合は draft に戻す。表記修正や、未理解を明示した Open questions の追記だけでは戻す必要はない。空欄の任意メタデータは削除する。

既存のファイル名・H1・aliases・本文を検索してから作成する。表記違いは aliases に統合する。重複の説明をコピーせず、正本へリンクする。ノート間のリンクは Markdown の `[表示名](相対パス.md)` 形式に統一する。パスはリンク元ファイルのあるフォルダを基準にし、拡張子 `.md` を含め、空白は `%20` にする。例として `30 Technologies/Rust.md` から `20 Concepts/Ownership.md` へは `[Ownership](../20%20Concepts/Ownership.md)` と書く。リンク先の実在と、相対パスが正しく解決されることを確認し、何を説明する／何に使われる関係かを短く添える。意味のない全相互リンクは不要。未作成の候補は通常のテキストで残し、空リンクや架空のリンクを作らない。改名・統合時は参照元も更新し、内容を失わない。

## 記述の深さ

テンプレートの全項目を埋めることを目的にしない。不要な節は削除できる。Issue は Problem と制約・アプローチ、Concept は Definition → Purpose → Mechanism、Technology は Summary・Motivation・主要な仕組みをまず成立させる。

事実、資料に基づく解釈、自分の仮説、未理解を区別する。設計者が述べた理由と自分の推測を混同しない。分からないことを見栄えのために補完しない。「自分が未理解」と「技術自体に未解決の課題がある」は分けて書く。

## 調査の範囲と停止規則

着手時に現在の問い、必要な説明・比較、対象バージョン、今回扱わない範囲を短く定める。時間・資料数の制約が指定されていれば従う。関連語の再帰的な調査は行わず、現在の問いに答えるのに不可欠な依存概念だけを必要な深さまで確認する。

次のいずれかで止める: (1) 問いへの回答と主要な根拠・限界が揃った、(2) 合意した時間・範囲に達した、(3) 入手できる資料では重要な不確実性が解消しない。追加資料で判断が変わらなくなったら網羅性のために続けない。残った疑問は対象ノートの Open questions に記録する。保存先となるノートがない問いは、対話のまとめで示す。

## References

各ノートの最終節を `References` とする。外部資料はこの節に直接リンクする。公式仕様・一次資料を優先し、読んで確認した資料だけを根拠として扱う。未読の資料候補は、関連ノートの Open questions に未読と明記して添える。

Markdown の形式: 脚注記法（Footnote）を用い、識別に役立つ slug（例: `[^template]`）を付与する。
- 本文: `本文の重要な主張[^slug]。`
- References 節: `[^slug]: [資料名](URL) — 該当節、確認した主張。版・確認日: YYYY-MM-DD（変化する資料の場合）。`
連番（`[^1]` 等）ではなく識別名（slug）を使うことで、資料を後から追加・挿入した際の番号ズレを防ぎ、本文との対応を明確にする。書籍は著者・書名・版・章／ページで特定できれば URL は不要。自分の例・考察はそう明記し、References がなければ「外部資料未確認」と書く。未確認の URL、引用、著者、ページを捏造しない。資料が矛盾する場合は版・時点・条件を比較し、未解決なら両説を残す。

Survey も末尾に References を持ち、統合に使った vault ノートを別節で列挙する。ノート参照だけで外部の事実の根拠を代替しない。必要な原資料を再確認する。

## 更新と対話

依頼で認められた作成・更新は進める。候補提示のみの依頼なら書き込まない。分類の曖昧さがノートの意味を変えるときだけ境界を確認する。通常の追記のたびに承認を取り直さない。ユーザーの文章・仮説・未解決点を黙って消さない。

対話や提示されたメモから、再利用できる理解を既存ノートと照合し、必要な Issue・Concept・Technology を直接作成・更新する。整理の結果が「既存ノートへの小さな追記だけ」「今回は何も作らない」でもよい。終了時には変更したノート、判断理由、残った問いを簡潔に伝える。
